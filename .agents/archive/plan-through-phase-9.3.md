# Archived plan and changelog (2026-09-26)

**Historical record, not the plan.** This is `plan.md` exactly as it stood when Phases 0 to 9.3 were built, kept for the *why* behind decisions (what was tried, what was measured, what went wrong). What is left to do is in [`../../plan.md`](../../plan.md); durable design decisions are in [`../architecture.md`](../architecture.md). Some sections below say "not started" or "pending"; they were true on the day and are superseded by the current plan.

---

# Terminal Launcher — Flutter MVP Plan

Goal: an Android home-screen launcher that *is* a terminal. Basic functionality first, then aliases, DSL chains, and similar features on the same foundation.

Guidance for agents: [AGENTS.md](AGENTS.md) and [.agents/](.agents/README.md).
Each phase ends with: `dart format`, `flutter analyze`, `flutter test` clean, and (where noted) a device check.

## Phase 0 — Project hygiene
- Project already created (`android_terminal_launcher`, app id `com.codedbykay.android_terminal_launcher`). Remove the counter template from `lib/main.dart` and `test/widget_test.dart`.
- Enable the extra lints listed in `.agents/flutter-best-practices.md` in `analysis_options.yaml`.
- Create the folder layout from `.agents/architecture.md`.
- Add the monospace font **as a bundled asset** (e.g. JetBrains Mono, OFL license) declared in `pubspec.yaml`. Do not use `google_fonts`: it downloads at runtime and a launcher must work offline/at boot.

## Phase 1 — Make the App a Launcher (test early)
- Manifest: keep the `MAIN` + `HOME` + `DEFAULT` intent filter (already present); change `launchMode` to `singleTask`.
- Add a `PopScope(canPop: false)` so back never exits.
- Dark launch theme so there's no white flash.
- Build, install on a real phone, choose it as the default Home app. Confirm Home returns to it. **Do this before building further.**

## Phase 2 — Package visibility (replaces old "Permissions")
- Add a `<queries>` block for `MAIN`/`LAUNCHER` intents (see `.agents/android-launcher.md`). This lists all launchable apps on Android 11+ without the Play-restricted `QUERY_ALL_PACKAGES`. Only add `QUERY_ALL_PACKAGES` if a later feature needs non-launchable packages.

## Phase 3 — Pure-Dart terminal core (moved before the UI)
Built and unit-tested with no Flutter, so the UI is thin.
- `Tokenizer`: split on whitespace → `ParsedInput(command, args)`. No quotes/chaining yet, but a dedicated type so they slot in later.
- `Command(name, aliases, description, usage, run)` and `CommandRegistry` (lookup by name or alias, case-insensitive).
- `CommandResult` sealed type: output lines / error / clear.
- `TerminalSession` (`ChangeNotifier`): log lines (capped), `submit(String)` → tokenize → lookup → run → append result.
- Commands: `list`, `open <app>`, `help` (generated from registry), `clear`.
- Errors: `unknown command: <input>`; `no app found matching <name>`; ambiguous `open` lists candidates instead of guessing.
- Matching for `open`: exact name → prefix → substring, case-insensitive.

## Phase 4 — App listing and launching
- `AppRepository` interface (`listApps()`, `launch(packageName)`) + `FakeAppRepository` for tests.
- Real implementation: small Kotlin `MethodChannel` (`queryIntentActivities` / `getLaunchIntentForPackage`). **Do not use `device_apps`** (discontinued 2021, breaks on modern Gradle). `installed_apps` is an acceptable fallback.
- Launchable apps only, exclude self, sorted by label, cached with explicit refresh.

## Phase 5 — Terminal UI shell
- `Scaffold`, black background, bundled monospace font, single `ThemeData`.
- Scrollable log (`ListView.builder`, auto-scroll to newest) above an undecorated `TextField` at the bottom that stays focused (re-focus after submit and on app resume). Handle keyboard insets.
- Prompt (e.g. `$ `) and blinking cursor in its own small widget (`AnimationController`/`Timer`, disposed) so the log doesn't rebuild.
- Wire the field to `TerminalSession.submit`. Widget tests with `FakeAppRepository`.

## Phase 6 — Real-device daily use
- Run as your launcher for a few days. Note which commands you actually reach for (battery, time, date, wifi, flashlight?) and add them as new `Command`s — each should be a single file plus a test.

## Roadmap v2 — daily-use features (from [features.md](features.md))
Goal: the user rarely has to leave the launcher. Phases 0–6 above are the finished MVP; these continue from Phase 7 and are ordered by what each one needs, easiest first. Each feature is a **provider**: one group of commands (plus its service behind an interface and a fake for tests) registered into the existing registry. Every provider aims for create/read/update/delete, or a hand-off to the owning app where Android gives no API. Same finish rule as before: format, analyze, test clean, small verified steps.

### Phase 7 — Foundations + offline features (no permissions, no native code)
**Status: complete (2026-09-25)** — apart from the items under "Still open" below.
Do first, in this order:
1. **On-device check of the existing MVP** (the pending item above). *Done by the user: looks good.* Nothing new is built on an unverified launcher.
2. **Quoted arguments** in `Tokenizer` (`note add "buy milk"`). Notes, SMS and calendar events all need it; keep `ParsedInput` stable.
3. **Provider model**: a `CommandProvider` that contributes commands and owns its service; `CommandContext` reaches services through it rather than growing a field per feature. Record the decision in `.agents/architecture.md`.
4. **`LocalStore` interface** (small key/value + JSON) with an in-memory fake; first implementation on `shared_preferences`, swappable in one file. Reused by themes, notes, alarms, aliases.
Then the three easiest features (all done):
- **Calculator + unit conversion** (`calc 2*(3+4)`, `convert 5 km mi`). Pure Dart: hand-written expression parser and a unit table. No dependency, no permission, no storage. Currency waits for Phase 8 (it needs network rates).
- **Theme manager** (`theme list|set <name>`): Light, Dark, Coffee, all 90s-retro palettes in `ui/theme.dart`; choice saved in the store. Keep the Android launch theme black so there's no flash.
- **Notes / todos** (`note add|list|show|edit|rm`, `todo add|done|list|rm`): full CRUD on the store; ids not indices so removal is stable.

**UI: separate what belongs to what** (asked for during Phase 7). Subtle first: a faint divider above each command's echoed input, and a thin rule down the left edge of its output and errors; the welcome banner stays plain. Colours derive from the theme, so all three themes get it for free. *First pass done.* Next passes, only if daily use asks for them: dim older blocks, collapse long output, a per-block timestamp on tap, a stronger accent for errors.

**Still open in Phase 7:** nothing blocking. Journal (dated entries) was in `features.md` beside notes; `note` already stores created/edited times, so it is a `note` view rather than a new store. Tap-to-copy on output lines would help with `calc`/`convert` results.

### Phase 8 — Network (adds only the `INTERNET` permission) — complete (2026-09-25)
`HttpFetcher` abstraction over `dart:io` (no new dependency), HTTPS only, with a strict fake for tests.
- **Improve `help`** (asked for during Phase 8): the old one-line-per-command list wrapped badly on a phone. Now three layers, all under ~36 columns: `help` = commands grouped by provider with names packed onto short rows and a hint at the bottom (where the eye lands first); `help <group>` = each command with its description; `help <command>` = usage forms, examples, notes, aliases. Commands gained optional `forms`/`examples`/`notes`; a test fails if any help line is wider than a phone screen. *Done.*
- **Swedish Text TV** (`texttv [page|name] [part]`, alias `tt`): `https://texttv.nu/api/get/<page>?app=android_terminal_launcher&includePlainTextContent=1`, prints `content_plain`. **Corrected from the earlier plan:** Inrikes is **101**, Utrikes **104** (not 104/105); page 200 (economy) was discontinued in 2024. Names: `nyheter` 100, `inrikes` 101, `utrikes` 104, `sport` 300, `väder` 400. Pages are 40 columns wide, wider than a phone, so a new **fixed-width grid** output (`CommandOutput.columns`) makes the UI shrink the font instead of wrapping; the calendar month view in Phase 9 will use it. Long pages have parts (`texttv 130 2`). *Done.*
- **Currency conversion**: extends `convert` (`convert 100 usd sek`, `convert currencies`). Frankfurter (ECB reference rates, no key), cached in the `LocalStore` for 6 h, and **falls back to the last saved copy when offline** (labelled "saved rates from <date>"), so the offline rule still holds. *Done.*
- **Weather, city-based** (`weather <city>`, `weather home [<city>|clear]`, `weather`): Open-Meteo geocoding + 5-day forecast, metric. A saved home city makes plain `weather` work. GPS remains Phase 9. *Done.*

Verified against the live services (not just fixtures). Still to do on a device: the `INTERNET` permission and the real `IoHttpFetcher` on Android, e.g. `texttv`, `weather gothenburg`, `convert 100 usd sek`, then again in flight mode to see the offline messages and the saved-rates fallback.

### Phase 9 — Runtime permissions + platform channels
Add a `PermissionService` abstraction first (ask, remember a denial, print how to grant it), then one channel handler per capability beside `AppsChannelHandler`.
**Status: in progress (started 2026-09-26).** Done: `PermissionService`, weather via GPS, calendar read-only views, contacts + calls, SMS. Next: the WhatsApp/Messenger hand-off (`msg <name> --wa "text"`, below), then Phase 10. Calendar create/update/delete (`event add`) is deliberately parked until the read-only `cal` has been tried on the device; it is contained (new commands in `CalendarProvider`, methods on `CalendarService`, a `WRITE_CALENDAR` row in `PermissionsChannelHandler` and the manifest).
- **Weather via GPS** (`weather` with no argument): `ACCESS_COARSE_LOCATION` is enough. *Done in code; needs a device check* (see the changelog).
- **Calendar**: day/week/month views and `event add`. Read first, then create/update/delete. *Read-only views done in code (`cal`); `event add` and the rest still to do. Needs a device check.* Prefer a small `CalendarContract` channel like the apps one; check that `device_calendar` is maintained before depending on it.
- **Contacts + calls** (`call <name>`): `READ_CONTACTS`, then `ACTION_CALL` (`CALL_PHONE`), with `ACTION_DIAL` as the no-permission fallback. *Done in code (`contact`, `call`); needs a device check.*
- **SMS** (`sms <name|number> "text"`): `SEND_SMS` via `SmsManager` through our own channel (the `telephony` package is stale). Ordinary SMS only; see the WhatsApp/Messenger note below. *Done in code; needs a device check.*

### Phase 9.1 - Improvements
**Status: done in code (2026-09-26); needs a device look.** Details in the changelog.
- Further improve how text renders. Refer to images listed for guidance and inspiration. This is a good opportunity to deviate from our terminal 8-bit aesthetic for this command.
    - Images: (C:\Users\kaywi\dev\android_terminal_launcher\_temp_)
- Add three more themes (use your judgment):
    - Unicorn
    - Pastel (Pink Explosion)
    - Cyberpunk
- Replace the animated cat with an animated ASCII rocket taking off. Also, center the ASCII art on the screen and list.
- Add a feature where double-tapping the space bar moves the cursor after the last letter, inserts a period, and advances the cursor one space forward. Example: "Test sentence. "
- For the contact command add a list option. To list all contacts.

### Phase 9.2 - Add App Icon
**Status: done (2026-09-26).** The GPS/home question below is answered under the changelog.
- Use Icon (C:\Users\kaywi\dev\android_terminal_launcher\icons)
    - Use a background color that fits and hightens the image and overal launcher fell. 
- How does the relationship between GPS and the saved manual location work? Is GPS always used now when I run "Weather"? Previously, it fetched the stored home location. I lean toward using GPS as the default; if GPS is disabled, fall back to the stored home location. If that is also missing, prompt the user that a location must be provided (which I believe the command already does). GPS can also be overridden by supplying a specific location, such as "Weather Eskilstuna." Does that sound good?

### Phase 9.3 - Rich views (a richer look where a command has one)
A layer of structured, themed widgets for information that plain text shows badly (calendars, Text TV, later weather/help/contacts), which the user may switch off. Plain lines stay the source of truth. Agreed order:
1. **Foundation + calendar pilot.** *Done in code (2026-09-26); needs a device look.* `RichBlock` values in `CommandOutput.block`, `BlockView`, `ui rich|plain`, `cal --plain`, and `cal`/`cal week`/`cal day` drawn as a month card and an agenda.
2. **Text TV.** Real block graphics (logos, bars) and tappable page numbers, including prev/next. *Done in code (2026-09-26); needs a device look.*
3. **Roll-out** to weather, help, contacts and notes; a secondary accent and a dim colour per theme. *Weather and help done in code (2026-09-26); need a device look. Contacts, notes and the theme accents not started.*

### Phase 10 — Background work
- **Alarms and timers**: exact alarms (`SCHEDULE_EXACT_ALARM`), `POST_NOTIFICATIONS` on Android 13+, and re-scheduling after reboot. Needs a boot receiver and a persistent schedule in the store. Also needs an on-device test, since fake time can't cover doze behaviour.

### Phase 11 — Camera / QR
The first feature needing more than the log: an inline preview screen with a way back to the prompt. `camera` permission, `mobile_scanner`. Last because it forces a UI mode beyond the log; design that mode with the theme system by then.

**WhatsApp / Messenger:** they expose no API for sending from another app. What's possible is a hand-off: open the chat with the text pre-filled (`https://wa.me/<number>?text=…`, `https://m.me/<user>`) and the user taps Send. Reading or replying silently would need notification-listener access, which is fragile and invasive, so it is not planned. Do this as `msg <name> --wa "text"` in Phase 9.

## Later (intentionally NOT in the roadmap)
Aliases, `&&` chaining, up-arrow/command history, custom macros, config-file export, app-list refresh on install/uninstall. All build on the same tokenizer, registry, and session without rewriting them. History and aliases are cheap once the store exists; pull them forward if daily use asks for them.

## Changelog
- Reordered: terminal core now precedes UI so logic is testable without Flutter.
- Replaced `device_apps` (discontinued) with a `MethodChannel`/`AppRepository` abstraction.
- Replaced `QUERY_ALL_PACKAGES` with a `<queries>` launcher-intent block; corrected the "minSdk 21" claim (that permission is API 30 and is not a minSdk requirement).
- Replaced `google_fonts` with a bundled font asset (offline-safe).
- Added phase 0 (template cleanup, lints, layout) and back-button/`singleTask` handling for launcher behavior.
- Dropped the `flutter create terminal_launcher` step; the project already exists as `android_terminal_launcher`.
- Phases 0–2 implemented (2026-09-25): template removed, extra lints on, JetBrains Mono bundled, `singleTask` + `PopScope`, always-black launch theme, launcher `<queries>` block. Also dropped the unused `cupertino_icons` dependency and added `lib/messages.dart` for user-facing strings. The `terminal/` and `services/` dirs are created by phases 3–4 with their first files.
- Phases 3–5 implemented (2026-09-25): tokenizer, registry, session, `list`/`open`/`help`/`clear`, `AppRepository` + Kotlin `MethodChannel`, terminal UI. Deviations: no separate blinking-cursor widget (the `TextField` caret is used); `ChangeNotifier` import in `terminal_session.dart`; own-package exclusion lives in Dart. See the architecture decisions log.
- Added `refresh` (re-queries the cached app list) and `date`/`time` (2026-09-25). `CommandContext` gained an injectable `now` clock (default `systemNow`) so time-based commands stay deterministic in tests; `TerminalSession` takes a `clock` and passes it through.
- Added auto-suggestions (2026-09-25): a strip above the prompt offers command names and, after `open `, matching apps (exact, prefix, then substring). Tapping fills the input; Enter still runs it. Was on the "Later" list as tab completion.
- Added `uninstall <app>` (2026-09-25): opens Android's own confirmation dialog via `ACTION_DELETE` (needs `REQUEST_DELETE_PACKAGES`); resolves the app exactly like `open` through a shared `resolveApp`. It cannot know whether the user confirmed, so the cached list keeps the app until `refresh`.
- **Pending:** the Kotlin channel and the terminal UI have only been compiled and unit/widget-tested; they still need an on-device check (`list`, `open`, keyboard stays up, own app not listed). Also phase 1's on-device check (set as default Home app, confirm Home returns to it and Back does nothing) has NOT been run — no Android device was connected. Do it before phase 3.
- Housekeeping (2026-09-25): the repo root is the Flutter project, so removed the stale `android_terminal_launcher/` subfolder references from the docs and replaced the template README. `TerminalSession.submit` now catches `Error`s as well as `Exception`s, and `suggest` backs off for 5 s after a failed app-list load.
- Roadmap v2 (2026-09-25): added Phases 7–11 from `features.md`, ordered by prerequisites (foundations and offline features, then network, runtime permissions, background work, camera). Quoted arguments moved out of "Later" into Phase 7 because notes, SMS and calendar need them. The main manifest still lacks `INTERNET`; Phase 8 adds it.
- Phase 7, step 2 done (2026-09-25): quoted arguments in `Tokenizer` (`"…"`, `'…'`, backslash escapes, empty args, unterminated-quote error). Step 1 (on-device check, run by the user) is still open.
- Phase 7, step 3 done (2026-09-25): `CommandProvider` + `CommandRegistry.fromProviders`; built-ins regrouped as `SystemProvider`/`AppsProvider`; `main.dart` builds the registry from `defaultProviders`. See `.agents/architecture.md` ("Adding a feature").
- Phase 7, step 4 done (2026-09-25): `LocalStore` interface, `SharedPreferencesLocalStore` (added `shared_preferences`, official and maintained; `shared_preferences_platform_interface` as a dev dependency for its in-memory test platform), `InMemoryLocalStore` fake, and a shared contract test. Not wired into `main.dart` yet; the theme manager is its first consumer. Only step 1 (on-device check, run by the user) is open in the foundations; calculator, themes and notes are next.
- Phase 7 complete (2026-09-25): on-device check passed (user); quoting, providers and `LocalStore` as above; **`calc`/`convert`** (pure-Dart expression parser and unit table incl. Swedish kitchen measures; `1,5` works as a decimal); **theme manager** (`theme [name]`: dark, light, coffee; saved in `LocalStore`; system bars follow the theme); **`note`/`todo`** (`EntryStore`: numbered, stable ids, calls serialised because the UI does not await commands); **block separation** in the log UI. `main.dart` is now async: it loads the saved theme before the first frame.
- Phase 8 complete (2026-09-25): `help` redesign (grouped, three layers, phone-width test); `HttpFetcher`/`IoHttpFetcher` + `INTERNET`; `texttv`, currency in `convert`, `weather`; fixed-width grid output. The plan's Text TV page numbers were wrong and are corrected above (Inrikes 101, Utrikes 104). `ToolsProvider` now takes the currency service, so it moved out of `defaultProviders` into `main.dart` with the new `InfoProvider`. Tests use real API responses saved under `test/fixtures/`.
- Pre-phase-9 polish (2026-09-26), asked for before starting runtime permissions: **`font [size]`** (small/normal/large/huge, same `AppearanceProvider`/`LocalStore` pattern as `theme`, scales the whole text theme so the log/prompt/suggestions/grids all move together); **fixed-width grids now fill the screen** instead of shrinking-only, and drop the block's left rule, so `texttv` reads edge to edge like an actual TV page instead of hugging the left edge with dead space on a typical phone (see `.agents/architecture.md`); **a small ASCII banner** (a blinking cat, a few frames then it settles) shows at startup and again after `clear`, instead of the screen going blank; `weather` gained suggestions for its `home`/`home clear` keywords. Not done: id suggestions for `note`/`todo` (`show`/`rm`/`done <id>`) and currency-code suggestions for `convert` were considered but skipped — both would need a new synchronous cache (entries, rates are loaded async today) disproportionate to this pass, and the former reverses an existing, deliberately-tested "nothing past the subcommand" decision.
- Phase 9, step 1 (2026-09-26): **`PermissionService`** (`AppPermission` names a capability, not an Android string; replies granted / denied / permanentlyDenied, the last decided in Kotlin from `shouldShowRequestPermissionRationale` after a refusal) and **`weather` with no city now uses the phone's position** (`LocationService`; coarse location from the network provider through `LocationChannelHandler`, `ACCESS_COARSE_LOCATION` only). The saved home city is now the *fallback* when there is no position (denied, location off, no fix), with a "no location, showing home" line; without a home, the failure says why and, when the dialog will no longer appear, where to switch the permission on. `weather home` and `weather <city>` behave as before and never ask for location. `InfoProvider` takes a `LocationService`. **Pending device check:** `weather` (dialog appears, forecast for "Current location"), refuse once then again, refuse permanently (instructions printed), location switched off in Android, flight mode.
- Phase 9, step 1 follow-up (2026-09-26, device check): GPS weather worked but showed no place, only "Current location". The Kotlin side now names the spot with Android's own `Geocoder` (no new third party; done only after a fix, 5 s limit, skipped when the phone has no geocoder or is offline) and `LocationFound` carries optional `name`/`region`/`country`. The weather header shows them ("Gothenburg, Västra Götaland County, Sweden"); without a name it shows the position itself ("Current location (57.71, 11.97)"), so there is always something to see.
- Phase 9, step 2 (2026-09-26): **`cal`** (alias `calendar`), read-only. `CalendarService` over a `CalendarContract.Instances` query (`CalendarChannelHandler`, `READ_CALENDAR`, repeating events already expanded, hidden calendars and cancelled events left out). Views: `cal` / `cal month [2026-10|this|next|last]` = a 28-column **fixed-width grid** (`>` today, `*` day with events); `cal day|today|tomorrow|yesterday|<date>` and `cal week [date]` = plain lines with `09:00-10:00 Title @ place`, `all day` first. Refactor: the "permission denied / is off / where to turn it on" messages are now generic (`Messages.permissionDenied` etc.) and shared with `weather`. **Pending device check:** `cal` (permission dialog, then the grid), `cal week`, `cal tomorrow`; all-day and multi-day events on the right days (Swedish holidays/birthdays calendars are a good test); a hidden calendar stays hidden; refuse permanently for the instructions. Next: `event add`, which also needs `WRITE_CALENDAR`.
- Phase 9, step 3 (2026-09-26): **`contact <name>`** (alias `contacts`) and **`call <name|number>`**. `ContactsService` reads every phone number through `ContactsContract` (`READ_CONTACTS`); matching is in Dart (`matchContacts`: whole name, start of name, start of any word, then anywhere, best tier only, so `andersson` finds `Anna Andersson` and åäö compare right) and the same person synced from two accounts is merged, their numbers de-duplicated by digits. `PhoneService` rings with `ACTION_CALL` when `CALL_PHONE` is granted and otherwise opens the dialer (`ACTION_DIAL`, needs nothing), so calling never dead-ends on permission; emergency numbers, which Android refuses to ordinary apps, take the same fallback. `call` never guesses: several people, or several numbers with no single mobile, are listed and nothing is rung; a typed number skips the phone book (and its permission) entirely. `cal`, `contact` and `call` share one help group, "personal", because a group per provider pushed `help`'s overview past its 16-line test. **Pending device check:** `contact <name>` (dialog, then numbers and labels), `call <name>` (dialog, then rings), refuse `CALL_PHONE` (dialer opens), `call 0701234567`, `call 112` (dialer, not a call), a name with several numbers, a name that matches several people. Next: `sms`.
- Phase 9, step 4 (2026-09-26): **`sms <name|number> "text"`**. `SmsService` over `SmsManager` (`SEND_SMS`, `SmsChannelHandler`); it reports success only once the network has accepted **every part** of the message (a "sent" broadcast per part), so no signal (`no network service`) or flight mode is an error rather than a text that silently went nowhere; no answer within 30 s is "not confirmed by the network; check before resending", never a success. Deliberately strict because a text cannot be recalled: exactly two arguments (so a name or text of several words must be quoted, and `sms anna on my way` prints usage instead of guessing where the name ends), a 1000-character guard, and recipients resolve exactly as for `call` (never guessed; a typed number skips the phone book). The resolution moved out of `call` into `resolveRecipient` (`terminal/commands/resolve_recipient.dart`) so both share it; `call`'s tests pass unchanged. `sms` joins the "personal" help group; there is no fallback on a refusal (unlike `call`, there is no dialer to hand over to). **Pending device check:** `sms <your own number> "test"` first, then to a contact by name; flight mode (should say so, not "sent"); refuse the permission (dialog, then instructions when permanent); a text long enough for several parts (e.g. 200 characters) arrives whole.

- Phase 9.1 (2026-09-26): **Text TV in colour** (`texttv`): the API's HTML (`content`) carries a colour class per stretch of text, so pages now come with their real colours: white/yellow/green/cyan text, blue title bars, underlined page-number links, and **double-height headlines** (`line DH`; the blank row under one is dropped, and the row is drawn twice as tall with stretched glyphs, like a teletext headline). New value types `StyledRun`/`TvColor` (`services/styled_text.dart`), `CommandOutput.styles` beside `columns`, `LogLine.runs`, and `ui/tv_row.dart`, which draws each row cell by cell on its own black screen in **every theme** (the palette is fixed, not the theme's), edge to edge. Deviation from the terminal look, as asked, only for grids that carry styles. The plain text stays the source of truth: if the HTML is missing or not the expected shape (any row not 40 wide, a part-count mismatch) the page falls back to the old plain grid, never half colour. **Not done:** the site draws big logo lettering and block graphics as small pictures (`bgImg`); those cells come out as blank cells in their colours (so the "SVT Text" logo is a plain blue band). Drawing them would need each picture decoded to a 2x3 pattern; the pictures carry their colours, so a table cannot be keyed on the class names alone, it would have to fetch them or reverse the picture hash. **Three themes** (`unicorn`: pink on indigo; `pastel`: raspberry on bubblegum pink, a light theme; `cyberpunk`: neon cyan on a purple-black night), all WCAG AA like the others; `theme` help notes now wrap. **Rocket** replaces the cat: an ASCII rocket ignites, climbs four rows and settles with idling engines (1.8 s, then the timer stops), the picture and caption centred (one `Text` of equal-width lines so it keeps its shape). **Double space** in the prompt gives `. ` (cursor after it, full stop moved to the end of the text), but never on a single word, so a command is never given a period, and never doubled after existing punctuation. **`contact list`** shows every name under a count (`contact <name>` gives the numbers).
- Phase 9.2 (2026-09-26): **App icon** from `icons/terminal_app_icon.jpg`. The JPEG has the checkerboard baked in, so `tool/make_app_icon.py` cuts the monitor out (flood fill from the edges) and writes an adaptive icon (monitor on a solid **phosphor green** `#2EE66B`, kept inside the 66dp safe zone so circle, squircle and teardrop masks all show it whole) plus legacy square icons for older Android. Green because it is the default theme's colour and the glow on the monitor's own screen, and the navy bezel stands out sharply on it (mint and violet were tried; violet muted the bezel, amber fought the screen). Not done: a monochrome layer for Android 13 themed icons (a silhouette of this monitor is a blob).
- **GPS versus the saved home (question in 9.2):** yes, that is exactly how `weather` works now. Plain `weather` uses the phone's position; if that is unavailable (permission refused, location switched off in Android, or no fix in time) it uses the saved home city and says so ("no location, showing home"); with neither, it says why and now offers both ways out: `weather <city>` for one look, `weather home <city>` to save one. `weather <city>` and `weather home ...` never touch GPS, so naming a place always overrides it.
- Phase 9.3 step 1 (2026-09-26): **Rich views, foundation + calendar pilot.** Research first: no teletext package exists on pub.dev; xterm.dart (a shell emulator) and termui (Dart CLI, no Flutter widgets) are the wrong shape; table_calendar/kalender/flutter_agenda are interactive date-pickers/schedulers that fight our theming and are heavy, syncfusion_flutter_calendar is commercially licensed. So: our own small widgets. **Model:** `terminal/blocks.dart` (pure values: `MonthBlock`, `AgendaBlock`, with a `command` behind everything tappable) in `CommandOutput.block`; the command still returns the plain `lines`, which stay the source of truth (plain mode, tests, copying). The session keeps the block as one `LogLine` (`LogLine.block`, `text` = plain lines joined) when the view mode is rich, and logs the lines when plain; the mode is read as each command finishes, so `ui plain` affects new output only, like a terminal. **Setting:** `ui rich|plain` (`ViewMode`, `ViewModeController` in the store, `ViewModeSettings` for commands; default rich); `cal --plain` for one call. I dropped the proposed `auto` mode: every command without a block is already plain, so it would have meant the same as rich. **Calendar:** `cal` is a month card (arrows to the neighbouring months, filled cell for today, up to three dots per day for its events, tap a day to run `cal day <date>`); `cal week`/`day` an agenda (a heading per day, tap to open it; events with a bar in the calendar's own colour, start and end stacked, titles and places wrapping under themselves; finished events dimmed and the one under way lit; a colour key when there are two or more calendars). Android's event colour (`EVENT_COLOR`, else `CALENDAR_COLOR`) now travels with `CalendarEvent.color`; `readableOn` lifts a colour that would vanish on the theme's background until it reaches contrast 3, keeping its hue. Secondary text is `blockDimAlpha` (0.8; 0.65 failed AA on the pastel theme, caught by a test), finished events `blockPastOpacity` (0.65). Taps run the command like typed input, echo included. The plain calendar text (the old `*` grid and week list) is unchanged. Tested: builders (`calendar_blocks_test`), session (`terminal_session_blocks_test`), `ui`/`cal` commands, and widgets across every theme and font size on a 320 px screen (no overflow, contrast). Not done here: nothing on-device yet.
- Phase 9.3 step 2 (2026-09-26): **Text TV block graphics and links.** The logos ("SVT Text", page titles, UR) and the yellow/red bars are drawn by the site as one tiny GIF per cell, named by the CRC-32 of its bytes. I first meant to bundle a table from a build-time crawl; instead found that the GIFs are plain 13x16, two-colour, standard-LZW files that a normal encoder reproduces **byte for byte** (28 of 28 samples), so `services/tv_mosaic.dart` builds all 3480 possible pictures (62 partly lit patterns x 56 colour pairs, plus 8 one-colour cells) on first use, hashes them, and looks the hash up. No data file to go stale, no download. I checked it against the whole site (pages 100-899 through the API): 181 of the 186 distinct pictures resolve; the other 5 are not block graphics (a sun-like symbol on the weather pages 421 and 714, and page 777's technical test card), and stay blank cells as before. `StyledRun` gained `mosaic` (one 6-bit pattern per blank cell, bit `row*2+column`) and `command`; the parser reads the picture address and the link's `href`; `TvRow` paints each lit sixth (rows 5/6/5 sixteenths, columns 6/7 thirteenths, as the site cuts them, a hair oversize so nothing seams), verified by reading rendered pixels back. **Links:** the underlined page numbers and the `prev`/`next`/`more:` footer tap to run `texttv <page>` like typed input (a tap within a cell of a link counts, since they are 3 cells wide). `ui plain` now also drops Text TV's colours (grid kept, session decides), so plain means plain everywhere. Not done: the sun/weather symbols; screen-reader actions for links.
- Phase 9.3 step 2 fix (2026-09-26, from a device look): the Text TV screen had no margin: the 40th column touched the right edge. The black screen now has a one-cell gutter each side (`tvGutterCells`), drawn black edge to edge, so the page's own bars stop short of the edges and text in the first or last column has room; the font is about 5% smaller to pay for it. Tap positions account for it. Tested by reading pixels back.
- Phase 9.3 step 2 fix 2 (2026-09-26, from a device look): the margins were not equal *to the eye*. Both gutters were one cell, but the site's own pages leave column 0 blank and use columns 1 to 39 (measured over 4600 rows of pages 100-899: column 0 has text on 5% of rows, column 1 on 45%, column 39 on 16%), so the left showed two blank cells to the right's one. The gutter is now 0 on the left and 1 on the right (`tvGutterLeftCells`, `tvGutterRightCells`); the page's blank column 0 is the left margin. A test draws a page shaped like the site's and measures the black either side of the bar (equal within 2 px, on a wide and a narrow screen).
- Phase 9.3 step 3a (2026-09-26): **Weather card.** `weather` (any form that gives a forecast) comes with a `WeatherBlock`: the place (and the `no location, showing home` note), the sky as a line drawing with the temperature large, feels-like/wind/humidity, then a row per day with its icon, low, high and rain, the low-to-high drawn as a bar on one scale for the whole week. Icons are drawn in code (`WeatherIconPainter`: sun, partly cloudy, cloud, fog, drizzle, rain, snow, thunder; one line colour, so every theme works, no assets); `weatherKind` groups the WMO codes. The rain figure is dropped when a big font leaves the bar no room; the temperature scales down rather than overflows. `--plain` (now shared with `cal` as `splitPlainFlag`) gives the old lines, which are unchanged and agree with the block (tested figure by figure). A test found a real bug in the bar (a clamp with an upper bound below its lower one when the track is narrower than a dot at the huge font), fixed by moving the dot in from the end. Tested across every theme and font size at 320 px, and rendered to look at (dark, pastel, huge).
- Bug fix (2026-09-26, from a device screenshot): a red screen, `sliver_multi_box_adaptor.dart: 'child == null || indexOf(child) > index' is not true`. **Cause** (reproduced in a test first): `TerminalSession.submit` appended the echoed command to the log at once but only told the UI after the command finished, so during a slow command (a network call like `weather`) the log was longer than the list had been told; a relayout (the keyboard moving) then made the lazily-building list read the live, longer log with a stale item count, shifting what each keyed child was and breaking their order. Older than the rich views, but slow commands and the animated banner make it likelier. **Fix, two layers:** the session now notifies right after the echo (the command appears at once too, which it did not before), so the log never changes without an announcement; and `TerminalLog` builds from a snapshot of the lines, so nothing done in between can shift it. Tests: the echo shows while a command runs; a keyboard opening and closing over 60 lines of history while a command runs; two commands in flight; and the session's guarantee that the log is never longer than the last announcement said. (The old test that pinned one notification per submit is replaced: two is now correct.)
- Rocket (2026-09-26, from a device look): it now climbs 10 rows (was 4), faster as it gathers speed, still about 2.4 s and ending at the top of the picture.
- Phase 9.3 step 3b (2026-09-26): **Help.** `help` is a card of every group with its commands as chips; `help <group>` lists the commands with what they do (a row opens that command); `help <command>` shows the usage forms and the examples in tinted boxes, notes joined back into paragraphs that wrap to the width (`joinWrappedNotes`; they were hand-wrapped for 36 columns), and the aliases. Everything tappable runs `help <name>` like typed input. **Examples are deliberately not tappable**: one can send a text or place a call, so they only ever run by being typed. The text lines are unchanged; `help --plain` gives them. A test walks every chip and group name in the real overview and checks it opens real help. Tested across every theme and font size at 320 px.

<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Text TV, weather, currency rates (HTTPS only; nothing is cleartext) and
         `mail` (IMAP over TLS, port 993). -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Lets the `uninstall` command open Android's uninstall dialog (API 28+). -->
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />

    <!-- `weather` with no city: a rough position is enough for a forecast, so
         precise location is deliberately not requested. Asked for at runtime. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- `cal`: shows events from the phone's calendars. Asked for at runtime. -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <!-- `contact` and `call <name>`: look a name up in the phone book. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- `call`: place the call directly. Optional: when refused, the dialer
         opens with the number filled in instead. -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- `sms`: send an ordinary text message. There is no fallback for a refusal. -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />

            <!-- Required by Flutter CLI (`flutter run`) to discover and launch the app -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Makes the app selectable as a default Android home launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>
</manifest>
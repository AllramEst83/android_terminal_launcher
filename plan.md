# Terminal Launcher — Plan

Goal: an Android home-screen launcher that *is* a terminal, so the user rarely has to leave it. Plain terminal first; a richer look where information needs structure.

Guidance for agents: [AGENTS.md](AGENTS.md) and [.agents/](.agents/README.md). Each step ends with `dart format`, `flutter analyze`, `flutter test` clean and, where it touches the phone, a device check. Small steps: finish and verify one before starting the next.

The full history of Phases 0 to 9.3 (what was tried, measured and decided, with dates) is archived in [.agents/archive/plan-through-phase-9.3.md](.agents/archive/plan-through-phase-9.3.md). Durable design decisions live in [.agents/architecture.md](.agents/architecture.md).

## Done

| Phase | What it delivered |
|---|---|
| 0–2 | Hygiene, bundled font, launcher manifest (`singleTask`, back never exits, black launch theme), `<queries>` block instead of `QUERY_ALL_PACKAGES`. Checked on a device. |
| 3–5 | Pure-Dart core (tokenizer, registry, session), app listing/launching over a Kotlin channel (`list`, `open`, `refresh`, `uninstall`), terminal UI, `help`, `clear`, `date`, auto-suggestions. |
| 6 | Daily use on the real phone, ongoing (the user decides what to add). |
| 7 | Quoted arguments, provider model, `LocalStore`, `calc`/`convert`, `theme`, `note`/`todo`, block separation in the log. |
| 8 | `INTERNET`: `texttv`, currency in `convert`, `weather <city>`, `help` redesign, fixed-width grids. |
| 9 | Runtime permissions (`PermissionService`): GPS `weather` (named place, home fallback), `cal` (read-only), `contact`/`call`, `sms`. |
| 9.1 | Text TV in colour with double-height headlines, themes unicorn/pastel/cyberpunk, `font`, animated rocket banner (climbs 10 rows), double-space gives `. `, `contact list`. |
| 9.2 | App icon (adaptive, generated by `tool/make_app_icon.py`); GPS-versus-home behaviour settled. |
| 9.3 | **Rich views, fully migrated**: a `RichBlock` layer with `ui rich\|plain`. Cards for `cal`, `texttv` (real logos, tappable pages), `weather`, `help`, `contact`, `note`/`todo`, `list`, `calc`, `convert`, `date`, `theme`/`font`/`ui`, and a notice for every acknowledgement (`open`, `refresh`, `uninstall`, `call`, `sms`, `weather home`, ...). Pickers for "several match" (apps, people, numbers). Taps run what is harmless and **fill the prompt** for anything that acts or needs typing. |

Also fixed along the way: the red-screen `sliver_multi_box_adaptor` crash (a slow command left the log ahead of what the list had been told).

## Next, in order

1. **Phase 9.3, the last bit**: a secondary accent plus a dim colour per theme, so cards have more than one colour to group with (every card is one colour today).
2. **Calendar create/update/delete** (`event add`, …): `WRITE_CALENDAR`. Contained: new commands in `CalendarProvider`, methods on `CalendarService`, one row in `PermissionsChannelHandler` and the manifest. Parked until `cal` has had daily use.
3. **WhatsApp / Messenger hand-off**: `msg <name> --wa "text"` opens the chat with the text filled in (`wa.me`, `m.me`) and the user taps Send. They have no send API, and reading or replying silently would need notification-listener access, which is fragile and invasive, so that is not planned.
4. **Phase 10, alarms and timers**: `SCHEDULE_EXACT_ALARM`, `POST_NOTIFICATIONS` (Android 13+), a boot receiver and a persistent schedule to re-arm after reboot. Needs an on-device test; fake time cannot cover doze.
5. **Phase 11, camera / QR**: the first feature that needs more than the log (an inline preview with a way back to the prompt). `camera`, `mobile_scanner`. Last because it forces a UI mode beyond the log.

## Not yet checked on a device

Built and tested, but not recorded as tried on the phone: `sms` (test with your own number first), `call` and `contact`, the help cards, `ui plain`, the app icon, double-space, the flight-mode messages for `weather`/`texttv`/`convert`.

## Not implemented (on purpose or not yet)

- **Text TV**: the five pictures that are not block graphics (the weather sun symbol on pages 421/714, page 777's test card) stay blank; no screen-reader actions for its links.
- **App icon**: no monochrome layer for Android 13 themed icons (a silhouette of the monitor is a blob).
- **Suggestions**: no id completion for `note`/`todo` (`show`/`rm`/`done`), no currency codes for `convert`; both need a synchronous cache that is not worth the cost yet.
- **Notes**: no journal view, and no tap-to-copy on output lines (would help `calc`/`convert`).
- **Log UI**: no dimming of older blocks, collapsing of long output or timestamps on tap; only if daily use asks.
- **Errors**: a failed command is still red lines (the terminal look on purpose); only the ones that ask you to pick (several apps, people or numbers match) are cards.
- **`--plain` per command** exists on `cal`, `weather` and `help`; everywhere else `ui plain` turns the cards off.
- **View mode**: no `auto`; every command without a rich form is already plain, so it would mean the same as `rich`.
- **Calendar**: read-only for now (item 2 above).
- **Later, all cheap on the current tokenizer/registry/session**: aliases, `&&` chaining, command history, macros, config export, app-list refresh on install/uninstall.

## Changelog

- 2026-09-26: plan condensed now that Phases 0–9.3 are built. The old plan and its detailed changelog moved unedited to `.agents/archive/plan-through-phase-9.3.md`; this file now holds only status, what is next, and what was left out.
- 2026-09-26: full migration to rich views. Every command that prints data or an acknowledgement now has a card, with its plain lines unchanged (all earlier tests pass as they were). See `.agents/architecture.md` ("Rich blocks") for the rules, notably run-versus-fill.
